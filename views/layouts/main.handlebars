<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>inDev</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/style.css">

</head>

<body>
  <div class="d-flex flex-column justify-content-around">
    <header class="d-flex justify-content-between align-items-center p-2">
      <h1>
        <a href="/"><img src="/images/indevlogo.png" alt="logo" height="75" width="150"></a>
      </h1>
      <div class="d-flex align-items-center">
        <nav>
          <!-- Conditionally render login or logout links -->
          {{#if logged_in}}
          <a href="/api/artwork" class="no-button">main gallery</a> |
          <a href="/api/profile" class="no-button">profile</a> |
          <button class="no-button" id="logout">logout</button>
          {{else}}
          <a class="no-button" href="/login">login</a>
          {{/if}}
        </nav>
        <form action="/search/:result" class="search-form" method="get">
          <input type="text" id="search-bar" placeholder="Search for images by name...">
        </form>
      </div>

    </header>
    <main class="container container-fluid">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>

  </div>
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  <script src="/js/search.js"></script>
  {{/if}}
  <script src="/js/search.js"></script>
  </main>
  <footer>

  </footer>
  </div>
</body>

</html>